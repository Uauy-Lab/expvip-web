<%= form_for(@experiment_group) do |f| %>
  <% if @experiment_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment_group.errors.count, "error") %> prohibited this experiment_group from being saved:</h2>

      <ul>
      <% @experiment_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

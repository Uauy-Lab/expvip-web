# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#


default: &default
  adapter: mysql2
  encoding: utf8
  database: "expvip"
  username: expvip_db_user
  password: expvip_db_user_password
  host: localhost
  port: 3306

development:
  <<: *default
  database: "expvip_dev"
#  database: "expvip_berries"
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: "expvip_test"

production:
 <<: *default
 database: "expvip_prd"

#production:
#  <<: *default
#  database: db/production.sqlite3
